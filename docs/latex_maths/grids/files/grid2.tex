\documentclass[preview]{article}

\usepackage[pattern=majmin, majstep=10, minstep=2, colorset=std, bgcolor=white]{gridpapers}


% The `majmin` pattern in the `gridpapers` package is a combination of two patterns: major and minor. The number of squares in each pattern can be controlled by changing the values of `majstep` and `minstep`. The default values are 5 and 1 respectively.
\thispagestyle{empty}
\begin{document}
~
\end{document}